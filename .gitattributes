# Keep deployment scripts and config LF-only, regardless of a contributor's
# core.autocrlf setting — CRLF would break the shebang on Linux
# (`#!/usr/bin/env bash\r` -> "bad interpreter") and confuse systemd/nginx.
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
